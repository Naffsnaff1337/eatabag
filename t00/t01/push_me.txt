#git command1$ git add
# Adds the files in the local repository and stages them for commit. To unstge a file, use 'git reset HEAD YOU-FILE'.

#git command2$ git commint -m"NAME"
# Commits the tracked changes and prepares them to be pushed  to a remote repository. To remove this commit and modify the file, use 'git reset --soft HEAD~1' and add the file again.

#git command3$ git push origin master
# Pushes the changes in your local reposiitory up to the remote repository you specified as the origin.
